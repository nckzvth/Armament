namespace Armament.Persistence;

public static class PersistenceReadme
{
    public const string Status = "Persistence module active: EF Core + PostgreSQL migrations + transactional repositories.";
}
